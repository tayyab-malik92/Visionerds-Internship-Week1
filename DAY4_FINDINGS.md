# Day 4 - Production Ready Chatbot Findings

## Error Handling
- Used `try` and `except` to prevent application crashes.
- Displayed user-friendly error messages.
- Logged technical errors for debugging.

## Common API Errors
- Authentication Error
- Network Error
- Rate Limit Error
- Server Error

## Git History
- Learned to use `git log` and `git log --oneline`.
- Understood commit history and commit hashes.

## Merge Conflict
- Created and resolved a real merge conflict.
- Learned how Git conflict markers work.
- Manually resolved conflicts and completed the merge.

## Key Learning
- Production-ready applications should handle errors gracefully, and every developer should know how to resolve Git merge conflicts.